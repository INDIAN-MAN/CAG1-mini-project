<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>SG Liquor Store</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">SG Liquor Store</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/addLiquor">Add new Liquor</a>
            </li> 
          </ul>
        </div>
      </div>
    </nav>
  <div class="container">
    <br>
    <div class="text-center"><h2>Liquors from SG Liquor Store Database</h2></div>
    <br>
  
    <table class="table table-hover small text-center">
      <thead>
          <tr>
            <th width="100">Liquor Type</th>
            <th width="100">Liquor Name</th>
            <th width="100">Liquor Image</th>
            <th width="50">Years</th>
            <th width="50">Price</th>
            <th width="50">Quantity</th>
            <th width="50">Edit</th>
            <th width="50">Delete</th>
          </tr>
      </thead>
      <tbody>
        <% for(let i=0; i < Liquors.length; i++) { %>
          <tr>
            <td><%= Liquors[i].LiquorType %></td>
            <td><a href="/Liquor/<%= Liquors[i].LiquorId %>" ><%= Liquors[i].LiquorName %></a></td>
            <td><img src = "/images/<%= Liquors[i].image %>" width="20%"></td>
            <td><%= Liquors[i].Years %></td>
            <td>$<%= Liquors[i].Price %></td>
            <td><%= Liquors[i].Quantity %></td>
            <td><a href="/editLiquor/<%= Liquors[i].LiquorId %>">Edit</a></td>
            <td><a href="/deleteLiquor/<%= Liquors[i].LiquorId %>" onclick="return confirm('Are you sure you want to delete this Liquor?')">Delete</a></td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
</body>
</html>
